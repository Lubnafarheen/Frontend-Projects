// Calculate the average of the numbers in an array of numbers

const numbers =[5, 7, 8, 15, 16, 28, 17];

let sum = 0;

numbers.forEach(number => {
  sum+= number ;
  return total = sum/numbers.length;
})



console.log(total)